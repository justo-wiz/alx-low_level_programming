#include "main.h"

/**
 * _isdigit - Check for digit (0 thru 9)
 * @c: The digit to be computed
 * Return: 1 if is a digit 0 if otherwise
 */

int _isdigit(int c)
{
	if (a >= 48 && a<= 57)
	{
	return (1);
	}
	return (0);
}
